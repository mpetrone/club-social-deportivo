import Typography from '@material-ui/core/Typography';

const NoContractDeployed = () => {

  return (
  <Typography variant="h6">
        Lo sentimos, DAO FC no se encuentra en la cadena a la que se encuentra conectado
    </Typography>
  );
}

export default NoContractDeployed;