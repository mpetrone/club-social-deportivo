import Typography from '@material-ui/core/Typography';

const NoConnection = () => {

  return (
	<Typography variant="h6">
        No estas conectado con Metamask!
    </Typography>
  );
}

export default NoConnection;